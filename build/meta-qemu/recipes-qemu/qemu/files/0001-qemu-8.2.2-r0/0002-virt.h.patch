--- a/include/hw/arm/virt.h
+++ b/include/hw/arm/virt.h
@@ -58,6 +58,11 @@
     VIRT_GIC_VCPU,
     VIRT_GIC_ITS,
     VIRT_GIC_REDIST,
+
+	VIRT_CLCD,
+	VIRT_KMI0,
+	VIRT_KMI1,
+
     VIRT_SMMU,
     VIRT_UART,
     VIRT_MMIO,
@@ -148,6 +153,17 @@
     bool tcg_its;
     bool virt;
     bool ras;
+
+    /*
+     * Boot ROM emulation. When bootrom_drive names a drive, the machine
+     * copies bootrom_size bytes from bootrom_offset of that drive to
+     * bootrom_load and starts the CPU there — what a real SoC ROM does with
+     * the medium it boots from. See virt_bootrom_setup().
+     */
+    char *bootrom_drive;
+    uint64_t bootrom_offset;
+    uint64_t bootrom_load;
+    uint64_t bootrom_size;
     bool mte;
     bool dtb_randomness;
     OnOffAuto acpi;
